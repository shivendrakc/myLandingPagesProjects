function imgSlider(Picture){
    document.querySelector('.starbucks').src = Picture;
  }

  function changeColor(color){
    const circle = document.querySelector('.circle');
    circle.style.background = color;
  }